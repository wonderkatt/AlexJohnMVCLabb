using System;
using System.Collections.Generic;
using System.Text;

namespace BloodBowlTeamManager
{
    public enum GeneralSkillsEnum { Block, Tackle}
    public enum AgilitySkillsEnum { Dodge, SideStep, Catch}
    public enum StrengthSkillsEnum { Guard, MightyBlow, ThickSkull}
    public enum PassingSkillsEnum { Pass, Leader, SureHands}
    public enum MutationsEnum { Claw, Tentacles}
    public enum SpecialSkillsEnum { Stunty, PrehensileTail, BoneHead, Loner, ThrowTeamMate}
  
}
